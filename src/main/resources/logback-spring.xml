<?xml version="1.0" encoding="UTF-8"?>
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <!--
    Criterio de DoD #9: el trace id sale en CADA linea sin que ningun filtro
    lo loguee a mano. Se puede seguir un request de punta a punta cruzando
    logs del Gateway y de un microservicio con un solo id.
  -->
  <!--
    El nombre de la aplicacion hay que declararlo: dentro de un
    logback-spring.xml, ${spring.application.name} NO se resuelve contra el
    Environment de Spring, y el campo sale vacio.
  -->
  <springProperty scope="context" name="appName" source="spring.application.name"/>

  <appender name="CONSOLA" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} %5p [${appName:-},%X{traceId:-},%X{spanId:-},%X{requestId:-}] %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLA"/>
  </root>
</configuration>
